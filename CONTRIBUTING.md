# Contributing to Thejas S's portfolio

Install dependencies with `npm install`, run `npm run dev`, and check changes with `npm run lint` and `npm run build`.

Keep personal details in `src/content/profile.js`, project data in `src/content/projects.js`, and images in `public/placeholders/` until replacement assets are available. Never commit credentials or claim placeholder experience as real.
